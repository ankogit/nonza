# Как создать комнату

## Способ 1: Через UI (рекомендуется)

1. **Откройте приложение:**

   ```
   http://localhost:3000
   ```

2. **На главном экране нажмите кнопку "Create New Room"**

3. **Заполните форму:**
   - **Room Name** - название комнаты (обязательно)
   - **Room Type** - выберите тип:
     - 🎤 **Conference Hall** - один основной спикер, остальные в сетке (для вебинаров, лекций)
     - 👥 **Round Table** - равноправные участники в круге (для совещаний)
   - **Temporary Room** - временная комната (автоматически истекает)
   - **Expires In** - время истечения (если выбрана временная комната)

4. **Нажмите "Create Room"**

5. **После создания:**
   - Отобразится экран с кодом комнаты (например: `abc-defg-hij`)
   - Можно скопировать код кнопкой "Copy"
   - Можно сразу присоединиться кнопкой "Join Room"

## Способ 2: Через URL параметр

Откройте в браузере:

```
http://localhost:3000?create=true
```

или

```
http://localhost:3000?action=create
```

## Способ 3: Через API (программно)

```bash
# 1. Создать организацию (если еще нет)
curl -X POST http://localhost:8000/api/v1/organizations \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My Organization",
    "description": "Test org"
  }'

# 2. Создать комнату
curl -X POST http://localhost:8000/api/v1/organizations/{orgId}/rooms \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My Room",
    "room_type": "conference_hall",
    "is_temporary": true,
    "expires_in": "1h"
  }'
```

## Что происходит при создании:

1. **Автоматически создается организация** (если еще нет)
   - Сохраняется в localStorage для повторного использования
   - Название: "Default Organization"

2. **Генерируется короткий код комнаты**
   - Формат: `abc-defg-hij` (3-4-3 буквы)
   - Уникальный код для быстрого доступа

3. **Создается LiveKit комната**
   - Автоматически генерируется `livekit_room_name`
   - Готова к подключению через WebRTC

4. **Возвращается информация о комнате:**
   - ID комнаты
   - Короткий код (`short_code`)
   - Тип комнаты
   - Время истечения (если временная)

## Типы комнат:

### Conference Hall (Конференц-зал)

- **Назначение:** Вебинары, лекции, презентации
- **Особенности:**
  - Один основной спикер (крупно)
  - Остальные участники в сетке
  - Подходит для асимметричных встреч

### Round Table (Круглый стол)

- **Назначение:** Совещания, обсуждения, командная работа
- **Особенности:**
  - Все участники равноправны
  - Круглое расположение видео
  - Подходит для симметричных встреч

## После создания комнаты:

1. **Поделитесь кодом** с участниками
2. **Участники вводят код** на главном экране
3. **Вводят свое имя** и нажимают "Join Room"
4. **Подключаются к LiveKit** через WebRTC

## Режим превью:

Для тестирования UI без бэкенда:

```
http://localhost:3000?preview=true&create=true
```

В режиме превью создается мок-комната с кодом `abc-defg-hij`.
